[user]
	name = Taylor Vance
	# Email intentionally unset here (work/personal split via per-repo config
	# or ~/.gitconfig.local); refuse to guess an identity instead
	useConfigOnly = true

[alias]
	st = status
	di = diff
	a = add .
	c = commit
	cm = commit -m
	s = switch
	f = fetch
	p = pull
	g = grep --break --heading --line-number
	l = log --graph --oneline --decorate
	ll = log --graph --pretty='%C(auto)%h%d %s %Cgreen(%cr)%Creset %C(bold blue)<%an>%Creset' --abbrev-commit
	cfg = config --list
	amend = commit --amend --no-edit
	# db = the remote's default branch (main/master/...); requires origin/HEAD
	db = "!ref=$(git symbolic-ref --short refs/remotes/origin/HEAD 2>/dev/null) || { echo 'origin/HEAD not set; run: git remote set-head origin -a' >&2; exit 1; }; echo \"${ref#origin/}\""
	smp = "!b=$(git db) && git switch \"$b\" && git pull --ff-only"
	fmom = "!b=$(git db) && git fetch && git merge \"origin/$b\""
	from = "!b=$(git db) && git fetch && git rebase \"origin/$b\""

[branch]
	sort = -committerdate

[commit]
	verbose = true

[push]
	autoSetupRemote = true

[fetch]
	prune = true

[pull]
	ff = only

[rebase]
	autostash = true
	autosquash = true

[rerere]
	# remember conflict resolutions; replay them when the same conflict reappears
	enabled = true

[diff]
	tool = nvimdiff
	colorMoved = default
	algorithm = histogram
[merge]
	tool = nvimdiff
	# include the common-ancestor version in conflict markers
	conflictStyle = zdiff3
[mergetool]
	keepBackup = false

[pager]
	diff = delta
	log = delta
	reflog = delta
	show = delta

[interactive]
	diffFilter = delta --color-only

[delta]
	line-numbers = true
	syntax-theme = tokyonight_moon
	file-decoration-style = magenta box
	hunk-header-decoration-style = cyan box

[core]
	editor = nvim
	excludesFile = ~/.gitignore
	pager = delta

[grep]
	extendRegexp = true
	lineNumber = true

[credential]
	helper = osxkeychain

[include]
	path = ~/.gitconfig.local
